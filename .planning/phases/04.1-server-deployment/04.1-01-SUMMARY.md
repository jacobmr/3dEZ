---
phase: 04.1-server-deployment
plan: "01"
subsystem: infra
tags: [docker, caddy, https, production, deployment, reverse-proxy]

# Dependency graph
requires:
  - phase: 01-foundation
    provides: backend Dockerfile, Docker dev environment, Makefile
  - phase: 04-3d-preview
    provides: working full-stack app to deploy
provides:
  - Production Docker configuration (backend + frontend + Caddy)
  - docker-compose.prod.yml for single-command deployment
  - Automatic HTTPS via Caddy + Let's Encrypt
  - make deploy/deploy-down/deploy-logs targets
affects: [server-deployment, ci-cd]

# Tech tracking
tech-stack:
  added: [caddy:2-alpine, node:20-alpine]
  patterns:
    [
      multi-stage Docker builds,
      Caddy reverse proxy with auto-HTTPS,
      standalone Next.js output,
    ]

key-files:
  created:
    [
      frontend/Dockerfile,
      docker-compose.prod.yml,
      Caddyfile,
      .env.production.example,
    ]
  modified: [backend/Dockerfile, Makefile, .gitignore]

key-decisions:
  - "Frontend build context at repo root (shared/ imports require monorepo context)"
  - "Caddy for reverse proxy + auto-HTTPS (simpler than nginx + certbot)"
  - "No external ports for backend/frontend — Caddy is sole entry point"

patterns-established:
  - "Multi-stage production Dockerfiles for both services"
  - "Caddy as edge reverse proxy routing /api/* to backend, /* to frontend"
  - "SQLite data volume for persistence across container restarts"

issues-created: []

# Metrics
duration: 13min
completed: 2026-03-09
---

# Phase 4.1 Plan 1: Server Deployment Summary

**Production Docker stack with Caddy auto-HTTPS reverse proxy, multi-stage Dockerfiles for backend and frontend, single-command deployment via make deploy**

## Performance

- **Duration:** 13 min
- **Started:** 2026-03-09T18:47:47Z
- **Completed:** 2026-03-09T19:01:13Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments

- Fixed backend production Dockerfile (added trimesh, correct OpenGL libs, hatchling build dep)
- Created frontend production Dockerfile with Next.js standalone output
- docker-compose.prod.yml orchestrates backend + frontend + Caddy reverse proxy
- Caddy auto-provisions Let's Encrypt HTTPS for ez3d.salundo.com
- Added deploy/deploy-down/deploy-logs Makefile targets

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix backend Dockerfile and create frontend production Dockerfile** - `d213b39` (feat)
2. **Task 2: Add docker-compose.prod.yml with Caddy reverse proxy** - `d144511` (feat)

## Files Created/Modified

- `backend/Dockerfile` - Fixed OpenGL libs (libgl1 instead of libgl1-mesa-glx), added trimesh and hatchling
- `frontend/Dockerfile` - New multi-stage production Dockerfile with standalone Next.js output
- `docker-compose.prod.yml` - Production compose with backend, frontend, caddy services
- `Caddyfile` - Reverse proxy routing /api/_ to backend, /_ to frontend with auto-HTTPS
- `.env.production.example` - Template with ANTHROPIC_API_KEY and DATABASE_URL
- `Makefile` - Added deploy, deploy-down, deploy-logs targets
- `.gitignore` - Added .env.production to prevent secret commits

## Decisions Made

- Frontend build context set to repo root (not ./frontend) — shared/ imports require monorepo context
- Caddy as reverse proxy with automatic HTTPS — simpler than nginx + certbot
- No external ports for backend/frontend services — Caddy is sole entry point
- SQLite data volume mounted at /app/data for persistence

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Frontend shared imports require repo-root build context**

- **Found during:** Task 1 (Frontend Dockerfile creation)
- **Issue:** Frontend imports from `@shared/api-types` which maps to `../shared/` — building from `./frontend` alone fails
- **Fix:** Set build context to repo root (`.`) with `dockerfile: frontend/Dockerfile`, adjusted COPY paths
- **Files modified:** frontend/Dockerfile, docker-compose.prod.yml
- **Verification:** Docker build succeeds with shared type imports resolved
- **Committed in:** d213b39

**2. [Rule 2 - Missing Critical] Added .env.production to .gitignore**

- **Found during:** Task 2 (env template creation)
- **Issue:** Production env file with secrets was not in .gitignore
- **Fix:** Added `.env.production` to .gitignore
- **Files modified:** .gitignore
- **Verification:** File excluded from git tracking
- **Committed in:** d144511

**3. [Rule 1 - Bug] Added hatchling build dependency to backend Dockerfile**

- **Found during:** Task 1 (Backend Dockerfile fix)
- **Issue:** Existing Dockerfile missing hatchling build backend that pyproject.toml requires
- **Fix:** Added hatchling to pip install in builder stage
- **Files modified:** backend/Dockerfile
- **Verification:** Backend Docker build succeeds
- **Committed in:** d213b39

---

**Total deviations:** 3 auto-fixed (1 bug, 1 missing critical, 1 blocking), 0 deferred
**Impact on plan:** All auto-fixes necessary for correct builds and security. No scope creep.

## Issues Encountered

None

## Next Phase Readiness

- Production Docker config ready — deploy with `make deploy` after creating .env.production on server
- Phase 4.1 complete (single plan phase)
- Ready for Phase 5: Photo Upload

---

_Phase: 04.1-server-deployment_
_Completed: 2026-03-09_
