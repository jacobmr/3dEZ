---
phase: 04.1-server-deployment
plan: "02"
subsystem: infra
tags: [provisioning, deploy, github-actions, sops, secrets, ci-cd]

# Dependency graph
requires:
  - phase: 04.1-server-deployment
    plan: "01"
    provides: docker-compose.prod.yml, Caddyfile, Dockerfiles
provides:
  - Server provisioned (Docker, firewall, fail2ban, swap)
  - GitHub Actions automated deploy pipeline
  - SOPS encrypted secrets management
  - Live app at https://ez3d.salundo.com
affects: [deployment, ci-cd, secrets]

# Tech tracking
tech-stack:
  added: [sops, age, github-actions]
  patterns:
    [server provisioning script, rsync deploy pipeline, SOPS encrypted secrets]

key-files:
  created:
    [
      scripts/provision-server.sh,
      .github/workflows/deploy.yml,
      .sops.yaml,
      secrets.production.env.enc,
    ]
  modified: [.env.production.example]

key-decisions:
  - "Direct SSH to Hetzner IP (37.27.198.218) for provisioning"
  - "SOPS + age for secrets management per inventory/docs/secrets-management.md"
  - "GitHub secrets for SSH_HOST, SSH_USER, SSH_KEY, ANTHROPIC_API_KEY"
  - "SSH config alias '3dez' added to ~/.ssh/config"

patterns-established:
  - "Server provisioning via idempotent bash script"
  - "GitHub Actions deploy: rsync + SSH docker compose"
  - "Encrypted secrets in repo (secrets.production.env.enc)"

issues-created: []

# Metrics
duration: 15min
completed: 2026-03-10
---

# Phase 4.1 Plan 2: Server Provisioning & Deploy Pipeline Summary

**Hetzner server provisioned with Docker, automated GitHub Actions deploy pipeline, SOPS encrypted secrets, live at https://ez3d.salundo.com**

## Performance

- **Duration:** 15 min
- **Started:** 2026-03-10T05:50:00Z
- **Completed:** 2026-03-10T06:10:00Z
- **Tasks:** 3
- **Files created/modified:** 5

## Accomplishments

- Created idempotent server provisioning script (Docker, ufw, fail2ban, swap)
- Provisioned Hetzner server at 37.27.198.218 (hostname: HuddleHHetzner1)
- Created GitHub Actions deploy workflow with rsync + SSH
- Set up SOPS + age encrypted secrets per project conventions
- Configured 4 GitHub secrets (SSH_HOST, SSH_USER, SSH_KEY, ANTHROPIC_API_KEY)
- First deployment successful — app live at https://ez3d.salundo.com with auto-HTTPS
- Added SSH config alias `3dez` to ~/.ssh/config

## Task Commits

1. **Task 1: Server provisioning script** - `06b7957` (feat)
2. **Task 2: GitHub Actions deploy workflow** - `13757dd` (feat)
3. **Task 3: SOPS secrets + GitHub secrets** - `4856aa9` (feat)

## Files Created/Modified

- `scripts/provision-server.sh` - Idempotent server provisioning (Docker, ufw, fail2ban, swap)
- `.github/workflows/deploy.yml` - Deploy on push to main via rsync + SSH
- `.sops.yaml` - SOPS config with age public key
- `secrets.production.env.enc` - Encrypted production secrets
- `.env.production.example` - Updated to stub pointing to encrypted file

## Decisions Made

- Direct SSH to Hetzner IP rather than ShellHub (no ShellHub agent installed yet)
- SOPS + age for secrets management following inventory/docs/secrets-management.md conventions
- Environment variables via GitHub secrets for deploy workflow

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] SOPS secrets management**

- **Found during:** Task 2 (Deploy workflow creation)
- **Issue:** Plan used raw SSH to write plaintext .env.production; project uses SOPS + age
- **Fix:** Created secrets.production.env.enc with SOPS, updated .env.production.example to stub
- **Files created:** .sops.yaml, secrets.production.env.enc
- **Committed in:** 4856aa9

---

**Total deviations:** 1 auto-fixed (1 missing critical), 0 deferred
**Impact on plan:** Aligned with project secrets conventions. No scope creep.

## Next Phase Readiness

- Server deployed and accessible at https://ez3d.salundo.com
- Automated deploy pipeline working (CI + Deploy on push to main)
- Ready for end-to-end testing (05-03 Task 3 checkpoint)
