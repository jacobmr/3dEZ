# Plan: 06-02 Multi-tenant Data Isolation

## Objective

Ensure per-user conversations, designs, and photos have complete ownership enforcement.

## Context

Most multi-tenant isolation was already implemented in 06-01 via `RequestContext.all_session_ids` across all API routes. This plan covers the remaining gap.

## Tasks

- [x] **Task 1: Photo ownership validation in message endpoint**
  Validate `photo_id` belongs to request context's sessions before passing to conversation service. Prevents cross-session photo injection.

## Notes

- 06-01 already implemented: RequestContext with all_session_ids, session claiming, dual-mode auth dependency, ownership guards on conversations/designs/photos routes
- STL generation is stateless (no storage, no ownership needed)
- Photo access is session-scoped (acceptable for V1)
