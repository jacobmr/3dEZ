# SECRETS MOVED TO ENCRYPTED STORAGE
# To decrypt: sops decrypt --input-type dotenv --output-type dotenv secrets.production.env.enc
#
# Required variables:
# ANTHROPIC_API_KEY - Anthropic API key for Claude conversation engine
# DATABASE_URL - SQLite database URL (default: sqlite+aiosqlite:///./data/3dez.db)
